# Firebase service account (JSON minified to single line) — optional if using file
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"seelo-acef3",...}

# Relay server
PORT=3001

# License keys are managed via: node relay/generate-key.js <planId> [days] [label]
# Plans: pro_monthly, pro_yearly, team_monthly, team_yearly
