node_modules/
.env
target/
build/
.dart_tool/
.pub-cache/
*.log

# Firebase service account (keep secret!)
relay/service-account.json

# Relay persistence (user data, plan info)
relay/store-data.json

# Keystore and signing credentials (keep secret!)
android/key.properties
*.jks
*.keystore
